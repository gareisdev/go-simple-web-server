# Simple Web Server with GO

## Description

This is a simple server that serves static files and pages written in pure HTML.

I use only the modules of `html/template` and `net/http`

### How run this project?

1. Check docker is up
2. Run docker-compose up
```bash
docker-compose up -d
```
3. Open a web browser and go to `http://127.0.0.1:8080`
